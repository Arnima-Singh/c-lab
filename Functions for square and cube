#include <stdio.h>
int square(int n) {
    return n*n;
}
int cube(int n) {
    return n*n*n;
}
int main() {
    int x = 3;
    printf("Square: %d\n", square(x));
    printf("Cube: %d\n", cube(x));
    return 0;
}
