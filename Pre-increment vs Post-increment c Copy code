#include <stdio.h>
int main() {
    int a = 5, b, c;
    b = ++a; 
    printf("After pre-increment: a=%d, b=%d\n", a, b);
    
    a = 5;
    c = a++;
    printf("After post-increment: a=%d, c=%d\n", a, c);
    return 0;
}
